apiVersion: v1
kind: Service
metadata:
  name: django-app-service
spec:
  type: NodePort
  selector:
    app: django
  ports:
    - port: 3003
      targetPort: {{ .Values.app.containerPort }}
      nodePort: 30003